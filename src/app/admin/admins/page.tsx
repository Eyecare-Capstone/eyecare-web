export default async function AdminsPage() {
  // const getData = await fetch("http://localhost:5000/ping");
  // const jsonData = await getData.json(); // Menunggu respons diuraikan sebagai JSON

  // console.log(jsonData); // Data JSON yang diambil dari API

  return (
    <div className="flex flex-col items-center justify-center h-screen ">
      <h1 className="text-2xl font-bold mt-3">Admin Page</h1>
    </div>
  );
}
