## Getting Started

run the development server:

```bash
npm install
npm run dev
```

run the landing-page server:
cd landing-page
http-server

run the cms server:

- cd eyecare-blog
- npm i
- run npm dev
